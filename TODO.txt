* draw yellow highlighted connections of a hovered node last, but before the points are drawn
* draw edge distance text after connections are drawn
* implement Dijkstra
    * fix prioQueue: undefined error
* better organize the webpage